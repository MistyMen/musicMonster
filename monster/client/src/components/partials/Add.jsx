import React, { Component } from 'react';

class Add extends Component {
  render() {
    return(
      <button
        className="AddButton">
        +
      </button>
    )
  }
}

export default Add;
